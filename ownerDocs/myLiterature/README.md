# myLiterature

Literature workspace. `pdfs/` is gitignored and never committed (copyrighted
source PDFs). `review/` holds the owner's reading notes. `manifest.json` is
a stub index of the literature set.
